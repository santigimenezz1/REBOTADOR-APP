rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite a todos leer y escribir, incluso si no están autenticados
      allow read, write: if true;
    }
  }
}



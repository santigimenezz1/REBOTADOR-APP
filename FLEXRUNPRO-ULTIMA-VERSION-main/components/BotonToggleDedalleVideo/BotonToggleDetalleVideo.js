import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        borderWidth:1,
        borderColor:"red",
        height:30,
        width:150
    }
})

export default styles